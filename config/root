from wubwubwub import config
from nevow import vhost

# To add virtual hosts
#
# exampleRoot = static.File('/var/vhosts/example')
# config.root.addHost('example.com', exampleRoot)

# Add a rule to rewrite /~foo/bar.html to /usrs/foo/bar.html
# so that the classical user URLs will work.
#from twisted.web import rewrite
#config.root.default = rewrite.RewriterResource(config.root.default, rewrite.tildeToUsers)
